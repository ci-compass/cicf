#!/bin/bash

# CICF Week 1 Exercise 1
#
# Wildcards
#
# Add a wildcard argument to the following command to only list files
# starting with the letter 'e'.
# Keep the `-1` option, which tells `ls` to list each file on its line.

ls -1
